# 2026-06-25

## code-commenter skill 迁到项目级 + 实操 DSATokenToKVPool
- 将 `code-commenter` skill 从 user scope 迁到**项目级** `.codebuddy/skills/code-commenter`（团队共享），补充了团队 5 点规范：类级宏观 docstring（对外接口 + 框架交互 + 调用链图）、函数级 docstring（调用链定位 + 详解）+ 行间、Emoji 分层强调、图文并茂（box 图）、多实现类横向对比、绝不污染原代码。验证 `Skill is valid!`（Windows 上 quick_validate.py 需 `python -X utf8` 绕过 gbk 编码 bug）。
- 用该 skill 给 `DSATokenToKVPool`（memory_pool.py 2556 起，继承 MLATokenToKVPool）写了完整注释：类级（接口框/与 MLA 对比框/indexer 调用链框）+ 每方法 docstring + 行间。read_lints 零新增错误。
- DSA 关键事实：= DeepSeek Sparse Attention（DeepseekSparseAttnBackend）。在 MLA 基础上多一份 `index_k_with_scale_buffer`（indexer 的 fp8 K+scale，1head×128dim，page 索引）；Indexer 先对全量历史 token 选 topk，MLA attention 只对 topk 算，复杂度 O(seq)→O(topk)。创建条件 is_deepseek_dsa & use_mla_backend；page_size 固定 64(CUDA)。move/offload/transfer 均重写为 latent+indexer 锁步。

## Git / .codebuddy
- `.codebuddy/` 当前未被 .gitignore 忽略（`git status` 显示 `?? .codebuddy/`），commit 时会随 `git add` 一起上传。
- 建议：`.codebuddy/skills/` 适合团队共享，但 `.codebuddy/memory/`（个人工作日志，易冲突）不宜共享。可在 .gitignore 加 `.codebuddy/*` + `!.codebuddy/skills/` 只共享 skills。（待用户确认是否加）
